{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile",
            "command": "g++",
            "args": ["${file}", "-o", "${fileBasenameNoExtension}/${fileBasenameNoExtension}.exe", "-ggdb"],
        },
        {
            "label": "CompileO2",
            "command": "g++",
            "args": ["${file}", "-o", "${fileDirname}/${fileBasenameNoExtension}.exe", "-O2", "-ggdb"],
        }
    ]
}